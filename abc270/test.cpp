#include <bits/stdc++.h>
using namespace std;
const int mod = 1000000007;
#define ll long long
using Graph = vector<vector<ll>>;

void func(Graph &G){

}

void tfunc(vector<ll> &X){

}

void ttfunc(vector<vector<ll>> &X){

}

void yuiha(vector<vector<int>> G){

}

vector<long long> ggg;

int main(int argc, const char * argv[]) {

  // vector<ll> ttt = {1,2,3};
  // ttt.resize(4,6);
  // ttt.push_back(1);
  // tfunc(ttt);
  // cout << "hello world" << endl;

  // grobal=vector<ll>(5);
  ggg = vector<ll>(5,1);
  ggg.assign(8,0);
  cout << ggg[3] << endl;
  cout << "hello world" << endl;

  map<string,ll> m;
  m["sohmei"]=1;
  m["sohmei"]++;

  vector<vector<int>> S;
  yuiha(S);

  cout << m["sohmei"] << endl;

  // vector<vector<int>> H = { {1,2,3},{4,5,6} };
  // yuiha(H);
  return 0;
}