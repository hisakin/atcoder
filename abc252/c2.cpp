#include <bits/stdc++.h>
using namespace std;
const int mod = 1000000007;
#define ll long long

int ctoi(char c) {
	if (c >= '0' && c <= '9') {
		return c - '0';
	}
	return 0;
}

int main(int argc, const char * argv[]) {

  ll N; cin >> N;
  vector<string> S(N);
  for(ll i=0;i<N;i++){
    cin  >>  S[i];
  }

  ll target;

  for(ll target=0;target<10;target++){
    for(ll cycle=0;cycle<N;cycle++){
      for(ll current_N=0;current_N<N; currnet_N++){
        for(ll current_t=0;current_t<10; current_t++){
          
      }
        
      }
    }
  }





}