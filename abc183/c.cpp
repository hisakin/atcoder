#include <bits/stdc++.h>
using namespace std;
const int mod = 1000000007;
#define ll long long


void dfs(ll node_i, vector<bool> Array, ll sum_time){

  bool searched=true;
  for(ll i=0;i<Array.size();i++){
    if(Array[i]==false){
      searched=false;
      break;
    }
  }

  if(searched) 

  if(Array[node_i]) return
}


int main(int argc, const char * argv[]) {








}